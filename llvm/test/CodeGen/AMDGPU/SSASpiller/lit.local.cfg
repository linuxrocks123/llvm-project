# Run the SSA spiller's final register-pressure verifier on every test in this
# directory, regardless of build type.
from lit.llvm.subst import ToolSubst

llc = ToolSubst("llc", unresolved="fatal")
config.substitutions.insert(0, (llc.regex, "llc -amdgpu-ssa-spiller-verify-rp"))
